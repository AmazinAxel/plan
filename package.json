{
  "name": "plan",
  "description": "Personal keyboard-driven planning app on Cloudflare Workers + KV.",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/seed-local.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260605.1",
    "sortablejs": "^1.15.2",
    "typescript": "5.9.3",
    "wrangler": "^4.98.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
